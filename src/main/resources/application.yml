spring:
  datasource:
    url: jdbc:sqlite:interview.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.sqlite.hibernate.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  jackson:
    time-zone: Asia/Shanghai

server:
  port: 8080
